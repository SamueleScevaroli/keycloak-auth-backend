logging:
  level:
    ROOT: INFO
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
    it.samusceva.backend: DEBUG
spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
